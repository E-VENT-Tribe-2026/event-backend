def generate_embedding(text: str) -> list[float] | None:
    """
    Placeholder - OpenAI embedding generation disabled for now.
    Replace with live implementation when OPENAI_API_KEY is configured.
    """
    return None